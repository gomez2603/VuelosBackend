import { HorariollegadaPipe } from './horariollegada.pipe';

describe('HorariollegadaPipe', () => {
  it('create an instance', () => {
    const pipe = new HorariollegadaPipe();
    expect(pipe).toBeTruthy();
  });
});
